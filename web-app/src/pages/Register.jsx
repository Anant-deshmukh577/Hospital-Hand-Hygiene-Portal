import RegisterForm from '../components/auth/RegisterForm';

const Register = () => {
  // Only render the self-contained RegisterForm component
  return <RegisterForm />;
};

export default Register;