import AuditorLoginForm from '../components/auth/AuditorLoginForm';

const AuditorLogin = () => {
  // Only render the self-contained AuditorLoginForm component
  return <AuditorLoginForm />;
};

export default AuditorLogin;