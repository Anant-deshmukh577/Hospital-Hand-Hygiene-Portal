import AdminLoginForm from '../components/auth/AdminLoginForm';

const AdminLogin = () => {
  // Only render the self-contained AdminLoginForm component
  return <AdminLoginForm />;
};

export default AdminLogin;